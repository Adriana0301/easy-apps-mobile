import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tasksWrapper: {
    overflow: 'hidden',
    marginTop: 29,
    paddingRight: 15,
    paddingLeft: 15,
  },
});
export default styles;
